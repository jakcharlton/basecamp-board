


    <div id="board">
        <div class="swimlane">
        <h2 class="title">To Do</h2>
        <ul id="sortable1" class="card-area">
         <% @todo.each do |todo| %>
         <li id="<%= todo.id %>" class="ui-state-default" >
                <%= "#{todo.list_name} : #{todo.display_title}" %>
        </li>
		<% end %>
        </ul>
        </div>
        <div class="swimlane"><h2 class="title">In Progress</h2>
	   <ul id="sortable2" class="card-area">
	     <% @in_progress.each do |todo| %>
         <li id="<%= todo.id %>"  class="ui-state-default">
                <%= "#{todo.list_name} : #{todo.display_title}" %>
        </li>
		<% end %>
	   </ul>
    </div>
    <div class="swimlane"><h2 class="title">Done</h2>
	<ul id="sortable3" class="card-area">
	    <% @done.each do |todo| %>
         <li id="<%= todo.id %>"  class="ui-state-default">
                <%= "#{todo.list_name} : #{todo.display_title}" %>
        </li>
		<% end %>
	</ul>
    </div>
    </div>



    <script>
    $(function() {
        $( ".cardarea" ).sortable({
            connectWith: ".cardarea"
        }).disableSelection();
    });
    </script>




